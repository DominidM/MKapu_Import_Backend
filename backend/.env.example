# ====================================
# CONFIGURACIÓN DEL SERVIDOR
# ====================================
PORT=3000
NODE_ENV=development

# ====================================
# CONFIGURACIÓN DE MYSQL
# ====================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_contraseña_aqui
DB_NAME=mydb
DB_PORT=3306

# ====================================
# JWT SECRET
# ====================================
# IMPORTANTE: En producción, genera un secreto fuerte y único
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=cambia_este_secreto_por_uno_super_seguro_en_produccion

# ====================================
# MICROSERVICIOS - PUERTOS
# ====================================
GATEWAY_PORT=3000
AUTH_SERVICE_PORT=3001
ADMIN_SERVICE_PORT=3003

# ====================================
# URLs DE MICROSERVICIOS
# ====================================
AUTH_SERVICE_URL=http://localhost:3001
ADMIN_SERVICE_URL=http://localhost:3003

# ====================================
# INSTRUCCIONES DE USO
# ====================================
# 1. Copia este archivo y renómbralo a .env
#    Comando: cp .env.example .env
#
# 2. Cambia DB_PASSWORD por tu contraseña de MySQL
#
# 3. En producción, genera un JWT_SECRET seguro:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 4. NUNCA subas el archivo .env a GitHub
#    Ya está incluido en .gitignore
#
# 5. Para verificar la conexión:
#    npm run test:db
# ====================================
